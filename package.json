{
  "name": "ultimate-seat-selector",
  "version": "1.0.0",
  "description": "An app that allows you to identify as present for class and allows teachers to check attendance.",
  "scripts": {
    "start": "npm install express && node index.js && serve -l 8080 client/build",
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "-": "^0.0.1",
    "express": "^4.18.2",
    "firebase": "^9.17.1",
    "firebase-admin": "^11.5.0",
    "save": "^2.9.0",
    "svgson": "^5.2.1"
  }
}
